[Unit]
Description=OpenStack Database Cleaner Service
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
User=root
ExecStart={{ os_janitor_script_path }} --all
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
